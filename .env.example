# Copier ce fichier en .env et remplir les valeurs
ADMIN_KEY=           # min 16 chars — générer: openssl rand -base64 16
JWT_SECRET_KEY=      # min 32 chars — générer: openssl rand -hex 32
GEMINI_API_KEY=      # depuis https://aistudio.google.com/
LOG_LEVEL=INFO
